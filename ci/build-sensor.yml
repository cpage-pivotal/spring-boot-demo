---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

inputs:
- name: spring-boot-demo

outputs:
- name: build-sensor-output

run:
  path: spring-boot-demo/ci/build-sensor.sh